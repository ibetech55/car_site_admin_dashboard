import { StringSeperatorPipe } from './string-seperator.pipe';

describe('StringSeperatorPipe', () => {
  it('create an instance', () => {
    const pipe = new StringSeperatorPipe();
    expect(pipe).toBeTruthy();
  });
});
